import React from 'react'

const Logo = (props) => {
  return (
    <div className='flex justify-center items-center '>
        <img src="Image/Logo simple.png" alt="" height={props.Height} width={props.Width} />
        
    </div>
  )
}

export default Logo